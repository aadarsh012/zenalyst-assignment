/**
 * The housing scheme itself: how many flats, the application window, and where in its lifecycle it
 * has got to.
 *
 * <p>The root record everything else hangs off. Applications, deduplication runs and eventually
 * the draw are all scoped to one scheme.
 */
package com.zenalyst.housing.scheme;
